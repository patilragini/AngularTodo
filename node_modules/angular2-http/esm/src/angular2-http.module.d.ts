import { ModuleWithProviders } from '@angular/core';
import { RequestOptions, BaseRequestOptions, XHRBackend } from '@angular/http';
import { StorageService, StorageConfig, CookieConfig } from './angular2-http-storage.service';
import { RedirectService, RedirectConfig } from './angular2-http-redirect.service';
import { JwtHttp } from './angular2-http.service';
export declare class DefaultRequestOptions extends BaseRequestOptions {
    constructor();
}
export declare function jwtHttpFactory(backend: XHRBackend, defaultOptions: RequestOptions, storage: StorageService, redirect: RedirectService): JwtHttp;
export declare class Http2Module {
    static forRoot(cookie: CookieConfig, storage: StorageConfig, redirect: RedirectConfig): ModuleWithProviders;
}
