import { StorageService } from './angular2-http-storage.service';
export declare class AuthService {
    private storage;
    constructor(storage: StorageService);
    setToken(token: string): void;
    removeToken(): void;
    isLoggedIn(): boolean;
    getUsername(): string;
    getTokenProperty(property: string): any;
    hasClaim(claimType: string, claimValue: string): boolean;
    private decodeToken(token?);
    private isTokenExpired(token?);
    private getTokenExpirationDate(token?);
}
