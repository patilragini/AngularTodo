import { Location } from '@angular/common';
import { Router } from '@angular/router';
export declare class RedirectConfig {
    pathToLogin: string;
}
export declare const RedirectConfigDefaults: RedirectConfig;
export declare class RedirectService {
    private location;
    private router;
    private config;
    private redirectToUrlAfterLogin;
    constructor(location: Location, router: Router, config?: RedirectConfig);
    restore(): void;
    save(): void;
}
